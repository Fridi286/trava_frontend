import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF007EA7);  // Blau
  static const secondary = Color(0xFF003459);
  static const background = Color(0xFF00171F);
  static const text = Color(0xFFFFFFFF);
  static const accent = Color(0xFF00A7E1);
  static const success = Color(0xFF00C853);
  static const error = Color(0xFFFF0000);
}
